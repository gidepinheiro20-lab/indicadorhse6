












services:
  - type: web
    name: indicadorhse6
    env: node
    buildCommand: npm ci --omit=dev && npm rebuild better-sqlite3
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
